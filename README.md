# homeassistant-node-exporter-docker

This stands up Prometheus's node_export.

Usage:

OpenSSH addon


ssh admin@homeassistant
apk add docker-compose


cd /share
git clone 
cd homeassistant-node-exporter-docker
docker-compose up -d
